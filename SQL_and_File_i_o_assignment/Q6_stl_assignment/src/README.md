*  Create matrix using STL vector of size NxN where N is odd number entered by user.
 *  Input values from user and
 *  check if its both diagonal sum is same or not.
 *  Print the message accordingly.
